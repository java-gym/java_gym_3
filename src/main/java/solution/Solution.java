package solution;

import java.util.Optional;

import javax.annotation.Nonnull;

import noedit.Data;
import noedit.DataStream;
import noedit.Registers;

public class Solution {

    /**
     * Given a stream of data, sizes of cache registers, and a data item to look up, return the cache register that the data is in.
     *
     * @implNote The maximum complexity is linear {@code (O(n))}.
     */
    @Nonnull
    public Optional<Data> solve(@Nonnull Registers registers, @Nonnull DataStream input, @Nonnull Data searchItem) {
        //TODO: Implement your solution here
        throw new UnsupportedOperationException("solution not yet implemented");
    }
}
